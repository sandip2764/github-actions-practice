name : hello github actions

on:

  push:

    branches:
      - main

jobs :

  create:

    runs-on: ubuntu-latest

    steps:
      - run: touch hello.txt
      - run: echo "hello from github actions!" > hello.txt
      - run: cat hello.txt
      - run: ls -ls
