name: learn varible concept in github actions

on:

  push:

    branches:
      - main

jobs:

  create:

    runs-on: ubuntu-latest

    env:
      AUTHOR: sandip

    steps:
      - run: echo "Application Name - ${{vars.APP_NAME}}"
      - run: echo "Version - ${{vars.VERSION}}"
      - run: echo "Author Name - $AUTHOR"
